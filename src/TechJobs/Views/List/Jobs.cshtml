<h1>@ViewBag.title</h1>

<h3>@ViewBag.jobs.Count Result(s)</h3>

@if (!ViewBag.Jobs.Count.Equals(0))
{

    @for (int a = 0; a < ViewBag.jobs.Count; a++)
    {<table class="job-listing">


            @foreach (KeyValuePair<string, string> b in ViewBag.jobs[a])
            {
                <tr>

                    <th>
                        <h6>
                            @b.Key
                        </h6>
                    </th>
                    <td>
                        <p>
                            @b.Value
                        </p>
                    </td>
                </tr>

            }
        </table>

        <br>
    }

}}
@if (ViewBag.Jobs.Count.Equals(0))
{
    <h2>There are no results to display.</h2>
}
<hr />
@{
    // TODO #3 - Use the same list display from Views/Search/Index.cshtml
}